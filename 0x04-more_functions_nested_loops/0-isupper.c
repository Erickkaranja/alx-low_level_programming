#include "main.h"
/**
 * _isupper - returns valueof character if supper
 * @c: the character to check
 * Return: 1 if supper otherwise 0
 */
int _isupper(int c)
{
	int c;

if (c >= 'A' && c <= 'Z')
	{
	return (1);
	}
	else
		return (0);

}
