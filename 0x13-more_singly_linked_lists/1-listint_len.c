#include <stdio.h>
#include "lists.h"

/**
*listint_len - prints the length of alinked list.
*@h: pointer to the first element of the linked list.
*
*Return: The number of nodes.
*
*/

size_t listint_len(const listint_t *h)
{
size_t nodes = 0;

while (h)
{
node++;
h = h->next;
}
return (nodes);
}
